# Configuration Management Plan

Firmware, configuration, and secrets are separate managed artifacts. Use versioned configuration, last-known-good rollback, staged wireless credential migration, semantic release identifiers, frequent Git commits, and recoverable tags.
