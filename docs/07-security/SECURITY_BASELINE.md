# Security Baseline

Reference controls: role-based authorization, command audit, authenticated envelopes, replay protection, secret-at-rest protection in production design, signed firmware, Secure Boot/Flash Encryption target controls, credential rotation/revocation, and default-deny privileged operations.

Host cryptographic models are not substitutes for ESP32 key provisioning/HW-backed verification.
